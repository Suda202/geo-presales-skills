# 回答级情绪审计暂停门禁

更新时间：2026-09-04

## 本轮变更

- 暂停 `geo-presales-report-audit` 对回答级 `sentiment` 的审计、判定与写回。
- 将 JSON 修改范围收窄为 `brand_rankings`；`sentiment` 必须逐值保持原值。
- 从默认触发、执行流程、结构化交付和金标运行器移除回答级情绪判断。
- 保留既有情绪辅助代码与样例，避免删除未来句子级方案可能复用的技术资产；它们不再属于当前 Audit 的可执行流程。

## 恢复条件

只有句子级情绪方案明确以下内容后，才重新引入情绪审计：句子切分与证据边界、品牌归属规则、句子级正中负判定、跨句聚合规则、人工复核入口、JSON/CSV 字段契约和独立金标回归。

## 验证结果

- 品牌与引用金标：12/12 通过。
- 单元测试：21/21 通过（含“补丁携带 `sentiment` 必须拒绝”）；截图依赖 Pillow 不可用，1 项按既有条件跳过。
- Skill 结构与 JSON 契约校验：通过。
- 静态检查确认默认流程、结构化交付和触发语料不再要求回答级情绪审计或 `sentiment` 写回。
