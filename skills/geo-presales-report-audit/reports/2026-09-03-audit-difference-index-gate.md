# 审计差异定位索引门禁

更新时间：2026-09-03

## 本轮变更

- 审计差异文档在逐条明细前增加“错误类型—记录编号索引”。
- 待修改索引至少覆盖修复失败、待修复和新增 Case；已修复与待验证必须另标状态。
- 报告 Case 使用 `Task + #记录编号`，多平台 JSON 使用 `platform + wordid`。
- 报告级问题没有单条记录号时标明 Task、模块或具体数字，不编造编号。
- 索引必须完整覆盖明细；无差异时明确写“无审计差异”。

## 验证结果

- 执行用例结构与新增定位用例：15/15 通过。
- 结构化金标：17/17 通过。
- 单元测试：20/20 通过。
- 触发评测：21/21 通过，precision=1.0，recall=1.0。
- Skill 结构校验：通过，无失败或警告。
- JSON 语法校验：`manifest.json`、`trigger_cases.json`、`execution_cases.json`、`semantic_config.json` 通过。

## 兼容性

- 未新增飞书字段，继续将 Task 与记录号写入现有 `Bad Case` 和 `复现证据`。
- 未改变错误类型、状态、模块和优先级的现有口径。
- 为修复两个既有触发边界样例，将 `badcase_regression` 语义权重从 0.35 调整为 0.37；阈值保持 0.42，非目标样例无误触发。
